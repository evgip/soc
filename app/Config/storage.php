<?php

declare(strict_types=1);

$basePath = dirname(__DIR__, 2);

return [
    /*
    |--------------------------------------------------------------------------
    | Диск по умолчанию
    |--------------------------------------------------------------------------
    | Этот диск используется, когда не указано имя диска явно.
    */
    'default' => 'local',

    /*
    |--------------------------------------------------------------------------
    | Конфигурация дисков
    |--------------------------------------------------------------------------
    | Каждый диск имеет свой драйвер, корневую директорию и настройки.
    | Можно добавить сколько угодно дисков: avatars, backups, temp и т.д.
    */
    'disks' => [
        // Приватный диск для внутренних файлов (логи, временные файлы)
        'local' => [
            'driver' => 'local',
            'root' => $basePath . '/storage/app',
            'visibility' => 'private',
        ],

        // Публичный диск для файлов, доступных по URL
        'public' => [
            'driver' => 'local',
            'root' => $basePath . '/public/storage',
            'visibility' => 'public',
            'url' => '/storage',
        ],

        // Отдельный диск для аватаров пользователей
        'avatars' => [
            'driver' => 'local',
            'root' => $basePath . '/public/uploads/avatars',
            'visibility' => 'public',
            'url' => '/uploads/avatars',
        ],
    ],
];